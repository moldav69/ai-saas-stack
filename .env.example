# Nome del progetto Docker Compose
COMPOSE_PROJECT_NAME=ai_saas

# ====================================
# Configurazione n8n
# ====================================
N8N_HOST=app.miodominio.com
N8N_PORT=5678
N8N_PROTOCOL=https

# 🔒 SICUREZZA: Genera questa chiave OBBLIGATORIAMENTE con il comando:
# openssl rand -hex 32
# NON lasciare vuota o con valori di esempio!
N8N_ENCRYPTION_KEY=

# Autenticazione Basic per n8n (OBBLIGATORIO per sicurezza)
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=CAMBIA_QUESTA_PASSWORD_SUBITO

# ====================================
# Configurazione AnythingLLM
# ====================================
# ⚠️ IMPORTANTE: JWT_SECRET e ENCRYPTION_KEY sono stati RIMOSSI!
#
# AnythingLLM ora genera automaticamente questi valori al primo avvio.
# Completa semplicemente il wizard di setup all'indirizzo:
# https://llm.tuodominio.com
#
# Il wizard ti permetterà di:
# - Creare account admin sicuro
# - Scegliere provider LLM (OpenAI, Anthropic, Ollama, etc)
# - Configurare embedding model
# - Creare primo workspace
#
# Le chiavi JWT/ENCRYPTION saranno salvate automaticamente nel database interno.

# Chiave API OpenAI (opzionale, puoi configurarla anche dall'interfaccia web)
OPENAI_API_KEY=

# 🔒 Privacy: Disabilita telemetria AnythingLLM
# Imposta a 'true' per NON inviare dati di utilizzo anonimi ai server di AnythingLLM
# Default: true (telemetria disabilitata per privacy)
DISABLE_TELEMETRY=true

# ====================================
# Configurazione Backup (rclone)
# ====================================
# Nome del remote rclone configurato (es. 'gdrive' per Google Drive)
RCLONE_REMOTE_NAME=gdrive

# Percorso della cartella sul remote dove salvare i backup
RCLONE_REMOTE_PATH=vps-backups

# Numero di giorni di retention per i backup locali (dopo vengono cancellati)
BACKUP_RETENTION_DAYS=7
